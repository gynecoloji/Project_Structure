# Analysis_<project_code> — Report Summary

Presentation-ready summary of the results in `04-analysis/Analysis_<project_code>/`.
Keep **only summary files** here — a `.pptx` deck and this `.md`. **No subfolders**,
and no raw or intermediate data.

## Results summarized
| Analysis (04-analysis) | Key result | Final figures |
|------------------------|------------|---------------|
| `01_diff_expression` | _summarize the key result_ | _e.g. volcano, PCA_ |

## Deck
- `Analysis_<project_code>_summary.pptx`

_Update this file (and the deck) whenever an analysis run changes._
